# Ne remonter que les vulnérabilités high/critical.
# Les modérées dans la chaîne de build Expo (uuid via xcode/@expo/config-plugins)
# sont acceptées — non-runtime, non-fixables sans downgrade SDK majeur.
audit-level=high
